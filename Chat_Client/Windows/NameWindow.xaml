<Window x:Class="Chat_Client.Windows.NameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Chat_Client.Windows"
        xmlns:vm="clr-namespace:Chat_Client.ViewModel"
        mc:Ignorable="d"
        Title="NameWindow" Height="100" Width="150" Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <Label Content="Enter your name" Grid.Row="0"/>
        <TextBox Text="{Binding EnteredName, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1"/>
        <Button Grid.Row="2" Content="Ok!" Command="{Binding OKCommand}"/>
    </Grid>

</Window>
